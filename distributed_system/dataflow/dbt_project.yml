name: 'dataflow'
version: '1.0.0'

profile: 'dataflow'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["data_tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  dataflow:
    gold:
      +materialized: table
    silver:
      +materialized: table
    bronze:
      +materialized: table